const Uspitem = (props) => (
  <div className="item team">
    <img alt="" className="image" src={props.imgsrc} />
    <div className="component-title title">{props.uspTitle}</div>
  </div>
);

export default Uspitem;
